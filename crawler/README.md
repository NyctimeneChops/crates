# crawler/ (stub)

Planned component. A SoundCloud catalog crawler that discovers and ingests tracks
into the database at a larger scale than the current manual ingestion.

Nothing is implemented here yet. This directory is a placeholder so the intended
repository layout is visible.
