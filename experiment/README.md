# experiment/ (stub)

Planned component. An offline experiment intended to test whether the
recommendation signals correlate with listening behaviour.

Nothing is implemented here yet. The experiment is blocked until there is a real
user base to draw behavioural data from (see the lab notebook, CR-001).
